package com.kfilesync.mobile.ui.screens.transfer

/**
 * Receive-confirm flow (T2.6).
 *
 * Currently rendered inline in [TransfersScreen] as 'IncomingRequestBanner'
 * — a card-style row instead of a modal dialog, matching the mock in
 * 'mobile-design/11-ui-design.md' §4.2 (the dialog form is reserved for
 * Phase 5's "stricter accept" polish where we want to block other input
 * until the user decides).
 */
private val ReceiveConfirmDialogPlaceholder = Unit