package com.kfilesync.mobile.ui.screens.transfer

/**
 * Send-file confirmation flow (T2.6).
 *
 * The send picker is rendered inline in [TransfersScreen] as
 * 'SendTargetDialog' because it's tightly coupled to the screen's
 * 'SendDialogState' machine. This file is intentionally a placeholder so
 * the file inventory in 'mobile-design/14-detailed-tasks.md' matches the
 * code layout; future polish (Phase 5) can extract the dialog here.
 */
private val SendFileDialogPlaceholder = Unit